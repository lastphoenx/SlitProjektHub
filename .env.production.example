# Optional: Produktion auf dem Server. Kopieren nach .env und anpassen (nicht committen).

OPENAI_API_KEY=
ANTHROPIC_API_KEY=
MISTRAL_API_KEY=

PYTHONUNBUFFERED=1
STREAMLIT_SERVER_HEADLESS=true
STREAMLIT_SERVER_MAXUPLOADSIZE=200

# Nur wenn externes Auth/Deploy-Stack — Standard-App nutzt config/auth.yaml
# DOMAIN=<your-domain>
# PUBLIC_HOST=<app-subdomain>.<your-domain>
