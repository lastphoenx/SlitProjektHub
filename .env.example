# Kopieren: cp .env.example .env — nur lokal auf dem Server/PC, nie committen.

OPENAI_API_KEY=
ANTHROPIC_API_KEY=
MISTRAL_API_KEY=

# Lokales Ollama — OpenAI-kompatibles API, nur LAN-URL auf dem Server setzen
# Beispiel: http://<ollama-host-lan-ip>:11434
OLLAMA_BASE_URL=
OLLAMA_API_KEY=ollama

# Optional: öffentliche App-URL(s) für CORS (kommagetrennt)
# PUBLIC_APP_ORIGINS=https://hub.example.com
